import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.lrima.smarthouseapp',
  appName: 'Smart House App',
  webDir: 'dist'
};

export default config;
